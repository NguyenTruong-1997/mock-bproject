import { DebounceClickDirectives } from './debounces.directive';

describe('DebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceClickDirectives();
    expect(directive).toBeTruthy();
  });
});
